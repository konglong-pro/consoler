# Agent Rule Archive

Archived agent rule files should live here after link-preserving migration.

Do not read archived agent rules by default. Current agent rules live in `docs/agents/current/` and are selected through `docs/active/current.md`.
