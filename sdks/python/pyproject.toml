[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "consoler-agent-sdk"
version = "0.1.0"
description = "Minimal Python SDK for out-of-process consoler agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "build>=1.2",
    "twine>=5.0",
]
